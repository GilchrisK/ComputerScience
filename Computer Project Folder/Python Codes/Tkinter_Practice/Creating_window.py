from tkinter import *

window = Tk() #creates an instance of a window


window.geometry("500x500") #This determines the length and width of your window
window.title("Projectiles")

window.config(background="light grey") #this changes the colour of the window background
                                #use "hex color picker" and copy and paste the code and put it in the speech marks to change the background to your chosen colour.
                                #e.g "#5cfcff"



window.mainloop() #this places the window on the computer screen